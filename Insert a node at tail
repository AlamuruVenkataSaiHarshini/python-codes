def insertNodeAtTail(head, data):
    nn=SinglyLinkedListNode(data)
    if head is None:
        head=nn
    else:
        i=head
        while i.next:
            i=i.next
        i.next=nn
    return head
